'use client';

export default function UnitDetailClient({ unit }) {
  // Placeholder for future interactive features
  // (e.g., star-level toggle, form switch, damage calculator link)
  return null;
}
